<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFInal_PrototipoV1"
        tools:targetApi="31">

        <!-- DetailActivity como pantalla de splash -->
        <activity
            android:name=".DetailACtivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity, se ejecuta después de DetailActivity -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false" />

        <activity android:name=".Modulos"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".CreditosAct"
            android:theme="@style/Theme.App.TransparentStatusBarCreditos"
            android:exported="false"/>

        <activity android:name=".ModulosIntermedio"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Memorama_Inter"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Cards_Inter"
            android:exported="false"/>

        <activity android:name=".EscogerInterm"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Maravillas_Inter"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Trayecto_Inter"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Peguntados_Inter"
            android:theme="@style/Theme.App.TransparentStatusBar"
            />

        <activity android:name=".FelicidadesInter"
            android:theme="@style/Theme.App.TransparentStatusBarGaPe"
            android:exported="false"/>

        <activity android:name=".derrota_Inter"
            android:theme="@style/Theme.App.TransparentStatusBarGaPe"
            android:exported="false"/>

        <activity android:name=".Manual"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

        <activity android:name=".Arrastrar_Inter"
            android:theme="@style/Theme.App.TransparentStatusBar"
            android:exported="false"/>

    </application>
</manifest>
